$.level = 2;

// @include './__log.jsx'

(function () {
    'use strict';    

    /** @type {CompItem} */
    var comp = app.project.activeItem;
    var layer = comp.layers[1];
    $.props(layer)
})();
